services:
  - type: web
    name: inventory-api
    runtime: docker
    rootDir: backend
    plan: free
    healthCheckPath: /health
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: inventory-db
          property: connectionString
      - key: CORS_ORIGINS
        value: https://stockroom-inventory-management.vercel.app

databases:
  - name: inventory-db
    plan: free
